version: '3'
services:
  myapp-main:
    image: airport_final_api:latest
    ports:
      - 8080:8080
    environment:
      - spring.datasource.url=jdbc:mysql://host.docker.internal:3306/airport_db_2025
      - spring.datasource.password=password
      - spring.datasource.username=root